name: wiki-page
description: Generate wiki pages for a batch of pre-prepared entities
version: 1

stages:
  - name: write-pages
    kind: analysis
    agent: writer-lite
    contract: wiki-page
    ralph:
      max_attempts: 3
    context:
      include:
        - input
