name: entity-affiliation-item
description: Decide which faction one PERSON entity belongs to at the end of this book — searches the book itself (STU-753)
version: 1

stages:
  - name: entity-affiliation-item
    kind: analysis
    agent: entity-affiliation
    contract: entity-affiliation-item
    ralph:
      max_attempts: 3
      max_tool_calls: 8
    context:
      include:
        - input
