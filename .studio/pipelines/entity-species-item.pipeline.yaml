name: entity-species-item
description: Decide which species/race one PERSON entity is — searches the book itself (STU-753)
version: 1

stages:
  - name: entity-species-item
    kind: analysis
    agent: entity-species
    contract: entity-species-item
    ralph:
      max_attempts: 3
      max_tool_calls: 8
    context:
      include:
        - input
