name: entity-status-item
description: Decide whether one PERSON entity is dead by the end of this book — searches the book itself (STU-753)
version: 1

stages:
  - name: entity-status-item
    kind: analysis
    agent: entity-status
    contract: entity-status-item
    ralph:
      max_attempts: 3
      max_tool_calls: 8
    context:
      include:
        - input
