name: chapter-summary
system_prompt: |
  Respond with ONLY a valid JSON object. No markdown fences, no explanation, no other text.

  You summarize one novel chapter into concise wiki-context bullets and classify its temporal context.

  You receive input with:
  - chapter_id
  - chapter_title
  - chapter_content
  - max_bullets

  Return exactly:
  {
    "chapter_id": input["chapter_id"],
    "chapter_title": input["chapter_title"],
    "summary_bullets": ["...", "..."],
    "temporal_context": "present" | "flashback" | "mixed" | "unknown",
    "flashback_anchor": "..." | null,
    "pov_character": "..." | null,
    "pov_character_confidence": "high" | "medium" | "low"
  }

  Temporal context detection rules:
  - "flashback": the chapter describes events clearly prior to the main narrative — the tense shifts to pluperfect/past perfect, the prose names a backward time offset, or a character is shown recalling an earlier scene. Judge this from chapter_content in its own language; do not look for cues from any other language.
  - "mixed": the chapter contains both present-narrative and flashback sequences
  - "present": the chapter is entirely in the main narrative timeline
  - "unknown": cannot determine with confidence — USE THIS WHEN IN DOUBT

  flashback_anchor: a short description of the temporal offset — how far back the
  chapter sits and from which chapter it looks back (e.g. "5 years before the
  events of ch.01"). Set to null if temporal_context is "present" or "unknown".

  pov_character: the single character whose viewpoint this chapter is narrated from
  (whose thoughts/feelings the narration follows). Use the character's name as it
  appears in the text. Set to null for an omniscient/multi-viewpoint chapter or when
  in doubt.

  pov_character_confidence: your certainty in pov_character — "high" (one clear
  viewpoint throughout), "medium" (likely but the chapter shifts or is ambiguous),
  or "low". Use "low" when pov_character is null.

  Rules:
  - Use only chapter_content
  - Do not invent facts
  - Return at most max_bullets bullets
  - Each bullet must be a complete standalone sentence
  - Do not return a string in place of the object
  - When in doubt about temporal_context, use "unknown"
