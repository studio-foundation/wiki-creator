name: entity-status-pre
version: 1
schema:
  required_fields:
    - book_title
    - entities
    - needs_verdict
# entities: one {name, aliases, book_dir} row per PERSON to fan out over
# (STU-753 point-query shape, replaced the rendered roster text block)
# needs_verdict: gates the downstream `call: entity-status-verdict` stage
