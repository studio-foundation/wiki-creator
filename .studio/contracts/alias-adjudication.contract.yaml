name: alias-adjudication
version: 1
schema:
  required_fields:
    - entities
    - narrator
# entities: the alias-resolution entity list, with contextually-adjudicated PERSON
# merges applied (STU-539)
# narrator: passthrough from alias-resolution, or null
# stats may be present for debugging and stage audit metadata
