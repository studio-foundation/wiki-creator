name: classify-relationships-pre
version: 1
schema:
  required_fields:
    - batches
    - prompt_fingerprint
    - needs_verdict
# batches: one map item per ~_BATCH_SIZE-pair batch (STU-751), each `{pairs: [...]}`
# needs_verdict: gates the downstream `call: classify-relationships-verdict` stage
