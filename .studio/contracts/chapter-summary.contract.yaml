name: chapter-summary
version: 1
schema:
  required_fields:
    - chapter_summaries
# chapter_summaries:
#   {
#     "<chapter_key>": {
#       "chapter_id": "ch01",
#       "chapter_title": "Chapter 1",
#       "summary_bullets": ["...", "...", "..."],
#       "summary_method": "extractive|llm|extractive_fallback",
#       "quality_flags": ["llm_invalid_response", "fallback_used", "low_signal"],
#       "temporal_context": "present"|"flashback"|"mixed"|"unknown",
#       "flashback_anchor": "..." | null
#     }
#   }
expected_outputs:
  files:
    # Written by scripts/chapter_summary.py (this stage). Glob: per-book slug,
    # cwd = repo root (STU-600, relocated from resolve-clusters in STU-457).
    - "library/*/*/processing_output/*/chapter_summaries.json"
