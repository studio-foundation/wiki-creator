name: entity-species-pre
version: 1
schema:
  required_fields:
    - book_title
    - entities
    - prompt_fingerprint
    - needs_verdict
# entities: per-PERSON fan-out items (name, aliases, book_dir), not a rendered roster (STU-753)
# needs_verdict: gates the downstream `call: entity-species-verdict` stage
